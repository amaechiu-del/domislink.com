version: "3.8"

services:
  gpt-model:
    image: ai/gpt-oss:latest
    container_name: gpt-oss
    ports:
      - "5000:5000"
    environment:
      - MODEL_NAME=gpt-oss-20b
      - TRANSFORMERS_CACHE=/models
    volumes:
      - ./models:/models
    restart: unless-stopped
