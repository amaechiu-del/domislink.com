<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>We're Under Maintenance</title>
  <style>
    :root {
      --bg: #0f172a;       /* slate-900 */
      --card: #111827;     /* gray-900 */
      --text: #e5e7eb;     /* gray-200 */
      --muted: #9ca3af;    /* gray-400 */
      --accent: #22c55e;   /* green-500 */
      --accent-2: #60a5fa; /* blue-400 */
    }

    * {
      box-sizing: border-box;
    }

    html, body {
      height: 100%;
      margin: 0;
      background: radial-gradient(1200px 600px at 10% 10%, rgba(34,197,94,0.12), transparent 60%),
                  radial-gradient(1200px 600px at 90% 90%, rgba(96,165,250,0.12), transparent 60%),
                  var(--bg);
      color: var(--text);
      font-family: ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, "Helvetica Neue", Arial, "Noto Sans", "Liberation Sans", sans-serif;
      line-height: 1.6;
    }

    .wrap {
      min-height: 100%;
      display: grid;
      place-items: center;
      padding: 24px;
    }

    .card {
      width: 100%;
      max-width: 720px;
      background: color-mix(in oklab, var(--card) 92%, black);
      border: 1px solid rgba(255, 255, 255, 0.06);
      border-radius: 20px;
      padding: 28px;
      backdrop-filter: blur(6px);
      box-shadow:
        0 30px 60px -20px rgba(0, 0, 0, 0.6),
        inset 0 1px 0 rgba(255, 255, 255, 0.04);
    }

    .media {
      display: grid;
      grid-template-columns: 1.2fr 1fr;
      gap: 24px;
      align-items: center;
    }

    .hero {
      width: 100%;
      aspect-ratio: 16 / 10;
      border-radius: 16px;
      overflow: hidden;
      border: 1px solid rgba(255, 255, 255, 0.06);
      background: linear-gradient(135deg, rgba(34,197,94,0.15), rgba(96,165,250,0.15));
    }

    .hero img {
      width: 100%;
      height: 100%;
      display: block;
      object-fit: cover;
    }

    .content h1 {
      font-size: clamp(1.6rem, 2.2vw + 1rem, 2.4rem);
      margin: 0 0 10px;
      letter-spacing: -0.02em;
    }

    .content p {
      margin: 0 0 18px;
      color: var(--muted);
      font-size: clamp(0.95rem, 0.8vw + 0.6rem, 1.05rem);
    }

    .status {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      padding: 10px 14px;
      border-radius: 999px;
      background: rgba(34, 197, 94, 0.12);
      border: 1px solid rgba(34, 197, 94, 0.35);
      color: #c7f9d4;
      font-weight: 600;
      letter-spacing: 0.02em;
    }

    .dot {
      width: 10px;
      height: 10px;
      border-radius: 50%;
      background:
        radial-gradient(circle at 50% 50%, #b7f6c9 10%, #22c55e 40%, #16a34a 100%);
      box-shadow:
        0 0 16px rgba(34, 197, 94, 0.8),
        0 0 4px rgba(34, 197, 94, 0.9) inset;
      animation: pulse 1.8s ease-in-out infinite;
    }

    @keyframes pulse {
      0%, 100% { transform: scale(1); opacity: 1; }
      50% { transform: scale(0.85); opacity: 0.7; }
    }

    .details {
      margin-top: 14px;
      display: grid;
      gap: 8px;
      color: var(--muted);
      font-size: 0.95rem;
    }

    .eta {
      display: inline-flex;
      align-items: center;
      gap: 10px;
      margin-top: 6px;
      color: #cfe5ff;
      background: rgba(96, 165, 250, 0.12);
      border: 1px solid rgba(96, 165, 250, 0.35);
      padding: 8px 12px;
      border-radius: 12px;
      font-weight: 500;
    }

    .tag {
      display: inline-block;
      font-size: 0.85rem;
      color: #9ec8ff;
      background: rgba(96, 165, 250, 0.1);
      border: 1px dashed rgba(96, 165, 250, 0.35);
      padding: 6px 10px;
      border-radius: 999px;
      margin-top: 8px;
    }

    .footer {
      margin-top: 22px;
      display: flex;
      gap: 10px;
      flex-wrap: wrap;
      align-items: center;
      color: var(--muted);
      font-size: 0.9rem;
    }

    .btn {
      display: inline-flex;
      align-items: center;
      gap: 10px;
      padding: 10px 14px;
      border-radius: 12px;
      border: 1px solid rgba(255, 255, 255, 0.08);
      color: var(--text);
      background: rgba(255, 255, 255, 0.06);
      text-decoration: none;
      transition: transform 160ms ease, background 160ms ease, border-color 160ms ease;
    }

    .btn:hover {
      transform: translateY(-1px);
      background: rgba(255, 255, 255, 0.1);
      border-color: rgba(255, 255, 255, 0.15);
    }

    @media (max-width: 720px) {
      .media {
        grid-template-columns: 1fr;
      }
      .hero {
        aspect-ratio: 16 / 9;
      }
    }
  </style>
</head>
<body>
  <main class="wrap" role="main">
    <section class="card" aria-labelledby="maint-title">
      <div class="media">
        <figure class="hero" aria-label="Maintenance illustration">
          <!-- Replace the src with your own image URL -->
          <img src="https://domislink.com/favicon.ico"
               alt="Website under maintenance: tools and construction"
               loading="lazy" />
        </figure>

        <div class="content">
          <h1 id="maint-title">We’re under maintenance</h1>
          <p>We’re performing some updates to make your experience better. The site is temporarily unavailable while we finish things up.</p>

          <div class="status">
            <span class="dot" aria-hidden="true"></span>
            <span>Maintenance in progress</span>
          </div>

          <div class="details">
            <span>Thank you for your patience — we’ll be back shortly.</span>
            <div class="eta" aria-live="polite">
              <span>Estimated return:</span>
              <strong>Soon</strong>
            </div>
            <span class="tag">Last updated: <time datetime="2025-12-06">Dec 6, 2025</time></span>
          </div>

          <div class="footer">
            <a class="btn" href="/" aria-label="Go to homepage">Home</a>
            <a class="btn" href="mailto:support@example.com" aria-label="Contact support">Contact support</a>
          </div>
        </div>
      </div>
    </section>
  </main>
</body>
</html>
